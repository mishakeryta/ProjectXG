<Window x:Class="ProjectXG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectXG"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Image Grid.Column ="1"   Source ="{Binding ShownImage  , Converter = {x:Static local:HeaderToImageConverter.Instance}}" />
        <StackPanel >
            <Button  Command="{Binding ClickAddCommand }" Content="Add"/>
            <Button  Command="{Binding ClickUpCommand }" Content="Up"/>
            <ListView Grid.Row="1"  ItemsSource="{Binding Items}" HorizontalContentAlignment="Center" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="3" Background="{Binding BackgroundColor ,Converter={x:Static local:HeaderToBackgroundConverter.Instance}}">
                            <StackPanel Orientation="Vertical" CanHorizontallyScroll="False" >
                                <Image  Margin="3" Width="52.5" Source="{Binding FullPath, Converter = {x:Static local:HeaderToImageConverter.Instance} }" />
                                <TextBlock Margin="3" Text="{Binding Name}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
            <Button  Command="{Binding ClickDownCommand }" Content="Down"/>
        </StackPanel>
    </Grid>
</Window>
